---
description: Rewrite the hero section (header in App.tsx) with bold DevOps engineer narrative — punchy headline, magnetic sub-headline, and a CTA that converts.
---

Read `src/App.tsx` lines 138–203 (the `<header id="home">` block).

Rewrite the hero section content with these constraints:
- **Headline**: Replace "Ghassen Khalfallah" display with a bold tagline *above* the name — something like "Infrastructure is code. Delivery is art." — that frames identity before the name lands.
- **Sub-headline**: Replace "Software Engineer | DevOps & Automation Specialist" with a punchy one-liner that communicates unique value (think: what does Ghassen *do* that others don't?). Reference the TPE/payment domain expertise and the Terminal Packager achievement.
- **Tech badges**: Reorder and reframe the 5 tech badges — lead with the most differentiating stack items, not the most common.
- **CTAs**: Rewrite "Let's Connect" and "About Me" with more intent-driven copy.
- **Tone**: Confident, technical, forward-leaning — not humble or vague.

Output the complete JSX replacement for the `<div className="max-w-4xl mx-auto text-center">` block only. Do not change layout classes — only the text content, badge list, and CTA labels.

Apply the changes directly to `src/App.tsx`.

$ARGUMENTS
